# 旧版15节点报告恢复证据

日期锚点：2026-08-22

## 结论

历史报告正文可由本地 Hermes 会话核对，但不等于 n8n 执行记录或 Telegram 服务器备份。

原始15节点 JSON、旧 n8n SQLite、执行记录和 Telegram 服务器备份均未找到。

因此 `n8n/okx-demo-risk-report.15-node-reconstructed.json` 是证据约束下的逆向重建版，不是字节级恢复，也未导入当前 n8n 数据库。

## 直接证据

- 归档内直接可见模板是3节点、2条连接。
- `examples/historical-telegram-20260728.json` 保存了2026-07-28历史报告原文。
- Hermes消息 `571701`、`571754`、`571822` 可交叉核对该报告。
- 源码直接支持持仓、4H K线、MA、Fib、新闻、模型、固定校验、纸面保护、报告和 Telegram 职责。
- `live_reporter.py` 的真实链路是：采集快照、生成报告、追加决策/保护状态 JSON。
- 当前 n8n 数据库：`quick_check=ok`、工作流2个、执行记录0。

## 逆向推理

15节点按已证实职责拆分为：

1. 每30分钟触发
2. 模拟盘只读安全门
3. 执行只读报告入口
4. 持仓缓存与过期标记
5. 核对实际持仓币种
6. 核对MA5/10/20/60
7. 核对局部Fib结构
8. 核对Grok新闻摘要
9. 汇总市场证据
10. 核对GPT风险分析
11. 独立止损安全校验
12. 止盈与Fib有效性校验
13. 生成中文风险报告
14. 报告字段与paper-only校验
15. Telegram末端节点

Python入口仍只执行一次真实采集。
后续节点解析和验收真实 stdout。
不会伪装成15次独立外部API调用。

节点名称、坐标、连接和参数属于推导。
不属于原始拓扑直接证据。

## 三版差异

|版本|当前状态|证据性质|用途|
|---|---|---|---|
|3节点|数据库保留、停用|归档直接证据|最小模板|
|5节点|数据库保留、停用|推导实现|职责展开|
|15节点|仅文件、未导入|逆向重建|逐段解析真实输出|

## stdout契约

入口命令显式覆盖以下安全值：

- `OKX_TRADING_MODE=demo`
- `PROTECTION_EXECUTION_ENABLED=false`
- `ACTIVE_CLOSE_EXECUTION_ENABLED=false`

stdout必须满足：

1. 以 `【OKX模拟盘` 开头。
2. 包含“不执行交易”。
3. 正文后的逐行 JSON 只进入 metadata。
4. 包含实际 `*-SWAP` 持仓。
5. 包含 MA5、MA10、MA20、MA60。
6. Fib不足时只能明确无效，禁止伪造。
7. 包含新闻状态、风险等级、止损和止盈校验。
8. 拒绝实盘、live模式和交易端点文本。

历史夹具新闻字段是正文描述：

`无有效新鲜新闻可直接用于ETH当前合约持仓判断；传入news_summary为reused`

15节点已兼容该格式和显式新闻字段格式。

## 验证结果

- JSON结构检查通过。
- 15节点、14条线性连接通过。
- `active=false` 通过。
- 无下单、撤单和交易端点通过。
- 所有 Code 节点 `node --check` 通过。
- 历史 stdout 逐节点验证通过。
- 缺少“不执行交易”时失败关闭通过。
- Python回归测试 `74/74` 通过。
- n8n健康接口返回 `{"status":"ok"}`。
- 当前执行记录仍为 `0`。

## 未验证事项

- 原始15节点名称、坐标、版本和精确拓扑。
- 旧 n8n 执行ID、工作流ID和 Telegram 服务器消息。
- 真实外部 API 响应及 Telegram 发送结果。

## 安全状态

本次没有访问 OKX、新闻、模型或 Telegram。
没有下单、撤单或发送消息。
没有读取、输出或复制凭据值。
15节点未落库，不能称为已部署。
